GIZI MASAKAN — brand assets
Mark: "Catatan" (1a) — the nutrition number, and its rust footnote assumption underneath.
Wordmark: Gizi Masakan (serif, matches the app's own heading face).
Tagline: EVERY NUMBER ASSUMES A RECIPE
  alts: See what it assumed / The recipe behind the number

Palette
  Ink #1A160F   Paper #EFEBDE
  Navy (figure) #2C4F71   Rust (assumption) #A2502E

svg/        vector masters, four colour variants (ink/light/navy/rust) + favicon
icon/       16-1024, maskable-512, colour variants (ink/light/navy/rust)
lockup/     horizontal + vertical, light/dark, each with tagline and plain (notag/plain),
            plus ink-on-transparent
wordmark/   ink, paper, duotone (Gizi in navy / Masakan in ink)
social/     og image 1200x630

Rules
  Navy is always the hard nutrition figure; rust is always the assumption underneath it —
  never swapped, never used decoratively elsewhere.
  The footnote dot only appears at 64px and above; at 32px the rust line runs solid, no dash.
  No gradients or shadows in the icon fills. Flat fills only.

HTML
  <link rel="icon" href="/favicon.svg">
  <link rel="apple-touch-icon" href="/gizi-masakan-icon-180.png">
